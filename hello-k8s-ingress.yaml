apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: hello-k8s-ingress
  annotations:
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: k8s.fraczyk.eu
    http:
      paths:
      - path: /
        backend:
          serviceName: bgshop
          servicePort: 80
